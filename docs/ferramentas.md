# Ferramentas

DER SP: Autuações expõe 1 ferramenta (todas somente leitura).

### 1. `der_sp_autuacoes_consultar`
**Input**: `renavam` (opcional), `login_cpf` (opcional), `login_senha` (opcional), `pkcs12_cert` (opcional), `pkcs12_pass` (opcional)

DER SP: Autuações, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DER SP: Autuações
```
